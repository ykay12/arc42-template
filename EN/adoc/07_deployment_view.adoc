=== Deployment Diagram
 
[source]
----
┌─────────────────────────────────────────────────────────────────────────────────────┐
│  CLIENT DEVICES                                                                     │
│                                                                                     │
│   ┌──────────────────────┐          ┌──────────────────────┐                        │
│   │  iOS App             │          │  Android App         │                        │
│   │  Swift / SwiftUI     │          │  Kotlin / Jetpack    │                        │
│   └──────────┬───────────┘          └──────────┬───────────┘                        │
└──────────────┼────────────────────────────────-┼────────────────────────────────────┘
               │ HTTPS                            │ HTTPS
               ▼                                 ▼
┌──────────────────────────────────┐   ┌──────────────────────────────────────────────┐
│  CDN / EDGE  (CloudFront)        │   │  API GATEWAY  (Kong)                         │
│  ┌────────────────────────────┐  │   │  ┌──────────────────────────────────────┐    │
│  │  Edge PoP                  │  │   │  │  Auth · Rate-limit · Routing · TLS   │    │
│  └────────────┬───────────────┘  │   └─────────────────────┼────────────────────────┘
└───────────────┼──────────────────┘     origin pull          │ HTTP/2 mTLS (internal)
                │ origin pull            ───────────►         │
                ▼                                             ▼
┌──────────────────────────────────────────────────────────────────────────────────────┐
│  APPLICATION CLUSTER  (AWS EKS / Kubernetes)                                         │
│                                                                                      │
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐     │
│  │ UserProfile Svc │ │ ImageFeed Svc   │ │ EditingEngine   │ │ CommunityHub    │     │
│  │ Pod x2          │ │ Pod x3          │ │ Svc  Pod x2     │ │ Svc  Pod x2     │     │
│  └────────┬────────┘ └────────┬────────┘ └────────┬────────┘ └────────┬────────┘     │
│           │                   │                    │ HTTPS             │             │
│           │          ┌────────┴────────┐           │ (Pixlr)          │              │
│           │          │ SubscriptionSvc │           ▼                  │              │
│           │          │ Pod x1          │    ┌─────────────┐           │              │
│           │          └────────┬────────┘    │  External   │           │              │
│           │                   │ HTTPS       │  Services   │           │              │
│           │                   │ (Stripe)    │  (see right)│           │              │
│           │                   │             └─────────────┘           │              │
│           │        ┌──────────┴──────────┐                            │              │
│           │        │   EntityManager     │◄───────────────────────────┘              │
│           │        │   Pod x2            │                                           │
│           │        └──────────┬──────────┘                                           │
│           │                   │                                                      │
│  ┌────────┴──────────────────────────┐     ┌────────────────────────┐                │
│  │  Message Queue  (SQS)             │────►│  Notification Worker   │                │
│  │  image.uploaded                   │     │  Pod x1                │                │
│  │  interaction.created              │     └────────────────────────┘                │
│  │  challenge.submitted              │                                               │
│  └───────────────────────────────────┘                                               │
└──────────────────────────────────────────────────────────────────────────────────────┘
               │ SQL/TCP                      │ HTTPS / SDK
               ▼                             ▼
┌─────────────────────────────────────────────────────────────────────────────────────┐
│  DATA LAYER  (private subnet — no public IPs)                                       │
│                                                                                     │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌──────────┐ ┌─────────────┐    │
│  │ PostgreSQL   │ │ PostgreSQL   │ │ Redis        │ │ S3       │ │ OpenSearch  │    │
│  │ Primary DB   │ │ Read Replica │ │ ElastiCache  │ │ (images) │ │ (search)    │    │
│  │ (RDS Multi-AZ│ │              │ │ sessions·    │ │          │ │             │    │
│  │  eu-west-1)  │ │              │ │ feed cache   │ │          │ │             │    │
│  └──────────────┘ └──────────────┘ └──────────────┘ └──────────┘ └─────────────┘    │
└─────────────────────────────────────────────────────────────────────────────────────┘
 
┌─────────────────────────────────────────┐   ┌────────────────────────────────────┐
│  EXTERNAL / THIRD-PARTY SERVICES        │   │  MONITORING & OBSERVABILITY        │
│                                         │   │                                    │
│  • Pixlr API  (photo editing SaaS)      │   │  • Prometheus + Grafana (metrics)  │
│  • Stripe  (payment gateway)            │   │  • Jaeger  (distributed tracing)   │
│  • Facebook / Instagram  (social share) │   │  • ELK Stack  (log aggregation)    │
│  • FCM / APNs  (push notifications)     │   │  • PagerDuty  (alerting)           │
│  • SendGrid / SES  (email)              │   │                                    │
│  • Apple / Google IAP  (subscriptions)  │   │                                    │
└─────────────────────────────────────────┘   └────────────────────────────────────┘
----
 
A draw.io version of this diagram is provided as a companion file `deployment_view.xml` (importable via Extras → Edit Diagram in diagrams.net).

=== Deployment Zones
 
[cols="2,4", options="header"]
|===
| Zone | Contents
 
| Client Devices
| iOS App (Swift / SwiftUI, distributed via App Store) and Android App (Kotlin / Jetpack Compose, distributed via Play Store).
 
| CDN / Edge (CloudFront)
| Global edge PoPs serving static assets (JS, fonts, app icons) and processed images. Origin pull from S3. Cache-Control headers set per asset type.
 
| API Gateway (Kong)
| Single entry point for all mobile API traffic. Responsibilities: JWT validation, rate limiting (per user and per IP), request routing to Kubernetes services, and TLS termination.
 
| Application Cluster (EKS / Kubernetes)
| All six microservices deployed as Kubernetes Deployments. HPA configured for UserProfile (min 2, max 8 pods), ImageFeed (min 3, max 12 pods), EditingEngine (min 2, max 6 pods), CommunityHub (min 2, max 8 pods), SubscriptionSvc (min 1, max 4 pods). NotificationWorker runs as a single replica with leader-election.
 
| Data Layer
a|
* PostgreSQL (Amazon RDS, Multi-AZ): primary write node + read replica per service schema.
* Redis (ElastiCache, cluster mode): session store, feed cache, rate-limit counters.
* S3: image and asset object storage; versioning enabled; lifecycle policy archives originals to Glacier after 365 days.
* Elasticsearch (Amazon OpenSearch): image tag and full-text search index.
* SQS: managed message queues per event type (image.uploaded, interaction.created, challenge.submitted).
 
| External Services
| Pixlr API, Stripe, Facebook/Instagram Share API, FCM, APNs, SendGrid.
 
| Monitoring & Observability
| Prometheus + Grafana for metrics; Jaeger for distributed tracing; ELK stack for log aggregation; PagerDuty for alerting.
|===
 
=== Network Topology
 
All inter-service communication within the Kubernetes cluster uses mTLS enforced by a service mesh (Istio). Services are not reachable from outside the cluster except through the API Gateway. The data layer sits in a private subnet with no public IP addresses. S3 access is via VPC endpoint (no public internet traversal).
 
